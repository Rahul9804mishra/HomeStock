import 'dotenv/config';import express from 'express';import cors from 'cors';import helmet from 'helmet';import morgan from 'morgan';import mongoSanitize from 'express-mongo-sanitize';import {connectDB} from './config/db.js';import auth from './routes/auth.js';import products from './routes/products.js';import stock from './routes/stockHistory.js';import purchases from './routes/purchases.js';import shopping from './routes/shopping.js';import reports from './routes/reports.js';import {notFound,errorHandler} from './middleware/error.js';
const app=express();app.use(helmet());app.use(cors({origin:process.env.CLIENT_URL?.split(',').map(x=>x.trim())||'*'}));app.use(express.json({limit:'1mb'}));app.use(express.urlencoded({extended:false}));app.use(mongoSanitize());app.use(morgan('dev'));app.get('/api/health',(req,res)=>res.json({ok:true,service:'HomeStock API'}));app.use('/api/auth',auth);app.use('/api/products',products);app.use('/api/stock-history',stock);app.use('/api/purchases',purchases);app.use('/api/shopping-list',shopping);app.use('/api/reports',reports);app.use(notFound);app.use(errorHandler);
const port=process.env.PORT||5000;connectDB().then(()=>app.listen(port,()=>console.log(`HomeStock API running on ${port}`))).catch(e=>{console.error(e);process.exit(1)});
