import {Router} from 'express';import {protect} from '../middleware/auth.js';import {list,create,update,remove,suggestions,purchaseToInventory} from '../controllers/shoppingController.js';const r=Router();r.use(protect);r.get('/',list).post('/',create);r.get('/suggestions',suggestions);r.put('/:id',update).delete('/:id',remove);r.post('/:id/purchase-to-inventory',purchaseToInventory);export default r;
