import {useEffect,useState} from 'react';import {api,msg} from '../services/api';import {fmt} from '../utils/constants';import {BarChart,Bar,LineChart,Line,PieChart,Pie,Cell,ResponsiveContainer,CartesianGrid,Tooltip,XAxis,YAxis,Legend} from 'recharts';import Toast from '../components/Toast';export default function Reports(){const [range,setRange]=useState('6m'),[data,setData]=useState(null),[inv,setInv]=useState(null),[toast,setToast]=useState('');useEffect(()=>{Promise.all([api.get('/reports/spending',{params:{range}}),api.get('/reports/inventory')]).then(([a,b])=>{setData(a.data);setInv(b.data)}).catch(e=>setToast(msg(e)))},[range]);if(!data)return <div className="p-8 text-center text-slate-500">Loading reports…</div>;return <div className="mx-auto max-w-7xl space-y-6"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm font-semibold text-brand-600">Reports</p><h1 className="text-3xl font-black">Grocery analytics</h1></div><select className="input w-auto" value={range} onChange={e=>setRange(e.target.value)}><option value="week">This week</option><option value="month">This month</option><option value="3m">Last 3 months</option><option value="6m">Last 6 months</option><option value="year">This year</option></select></div><div className="grid gap-6 lg:grid-cols-2"><Chart title="Monthly Grocery Spending"><ResponsiveContainer><LineChart data={data.monthly}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="_id"/><YAxis/><Tooltip formatter={v=>fmt(v)}/><Line type="monotone" dataKey="amount" strokeWidth={3}/></LineChart></ResponsiveContainer></Chart><Chart title="Category-wise Spending"><ResponsiveContainer><BarChart data={data.category}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="_id" angle={-20} textAnchor="end" height={65}/><YAxis/><Tooltip formatter={v=>fmt(v)}/><Bar dataKey="amount"/></BarChart></ResponsiveContainer></Chart><Chart title="Inventory Distribution"><ResponsiveContainer><PieChart><Pie data={inv.distribution} dataKey="quantity" nameKey="category" innerRadius={65} outerRadius={100}>{inv.distribution.map((_,i)=><Cell key={i}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></Chart><Chart title="Most Consumed Products"><ResponsiveContainer><BarChart data={inv.mostConsumed}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="product.name"/><YAxis/><Tooltip/><Bar dataKey="quantity"/></BarChart></ResponsiveContainer></Chart></div><div className="grid gap-4 sm:grid-cols-3"><Metric title="Inventory value" value={fmt(inv.inventoryValue)}/><Metric title="Low-stock frequency" value={inv.lowStockFrequency}/><Metric title="Expired products" value={inv.expiredProductCount}/></div><Toast message={toast} type="error"/></div>}
const Chart=({title,children})=><div className="card p-5"><h2 className="font-bold">{title}</h2><div className="mt-5 h-72">{children}</div></div>;const Metric=({title,value})=><div className="card p-5"><p className="text-sm text-slate-500">{title}</p><p className="mt-1 text-2xl font-black">{value}</p></div>;
