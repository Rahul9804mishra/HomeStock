import {useState} from 'react';import {api,msg} from '../services/api';import {useAuth} from '../context/AuthContext';import {Package} from 'lucide-react';import {useNavigate} from 'react-router-dom';import Toast from '../components/Toast';export default function Auth({mode}){const loginMode=mode==='login';const [form,setForm]=useState({name:'',email:'',password:''}),[loading,setLoading]=useState(false),[error,setError]=useState('');const {login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{const r=await api.post(`/auth/${loginMode?'login':'register'}`,form);login(r.data);nav('/')}catch(e){setError(msg(e))}finally{setLoading(false)}};return <div className="grid min-h-screen place-items-center bg-slate-50 p-4 dark:bg-slate-950"><div className="w-full max-w-md"><div className="mb-8 text-center"><div className="mx-auto mb-3 grid h-14 w-14 place-items-center rounded-2xl bg-brand-600 text-white"><Package/></div><h1 className="text-3xl font-black">HomeStock</h1><p className="mt-1 text-sm text-slate-500">Smart household grocery management</p></div><div className="card p-6"><h2 className="text-xl font-bold">{loginMode?'Welcome back':'Create your account'}</h2><p className="mt-1 text-sm text-slate-500">{loginMode?'Sign in to continue.':'Start managing your household stock.'}</p><form className="mt-6 space-y-4" onSubmit={submit}>{!loginMode&&<label className="block text-sm font-medium">Name<input className="input mt-1" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label>}<label className="block text-sm font-medium">Email<input className="input mt-1" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label className="block text-sm font-medium">Password<input className="input mt-1" type="password" minLength="8" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700 dark:bg-red-950/30 dark:text-red-300">{error}</p>}<button disabled={loading} className="btn-primary w-full">{loading?'Please wait…':loginMode?'Login':'Register'}</button></form><p className="mt-5 text-center text-sm text-slate-500">{loginMode?'No account? ':'Already registered? '}<button className="font-semibold text-brand-600" onClick={()=>nav(loginMode?'/register':'/login')}>{loginMode?'Register':'Login'}</button></p></div></div><Toast message=""/></div>}
