import Modal from './Modal';export default function Confirm({open,onClose,onConfirm,title='Delete item?',text='This action cannot be undone.'}){return <Modal open={open} onClose={onClose} title={title}><p className="text-sm text-slate-500">{text}</p><div className="mt-6 flex justify-end gap-3"><button className="btn-secondary" onClick={onClose}>Cancel</button><button className="btn-danger" onClick={onConfirm}>Delete</button></div></Modal>}
